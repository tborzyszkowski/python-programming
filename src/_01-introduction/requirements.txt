# Zależności dla modułu _01-introduction
# Instalacja: pip install -r requirements.txt

# Testy
pytest>=7.4
pytest-cov>=4.1

# Diagramy PlantUML (generowanie PNG przez serwer plantuml.com)
plantuml>=0.3
